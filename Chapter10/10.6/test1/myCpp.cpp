#include <iostream>

int main()
{
	/*
	类作用域
	在类中定义的名称(如类数据成员名和类成员函数名)的作用域都为整个类 作用域为整个类的名称只在该类中是已知的 在类外是不可知的 因此在不同类中使用相同的类成员名不会引起冲突
	另外 类作用域意味着不能从外部直接访问类的成员 公有成员函数也是如此 也就是 要调用公有成员函数 必须通过对象
	*/

	/*
	作用域为类的常量
	*/

	/*
	作用域内枚举(C++11)
	*/

	return 0;
}