#include <iostream>
using namespace std;

int main()
{
	/*
	循环和文本输入
	1.使用原始的cin进行输入
	cin >> ch;
	2.使用cin.get(char)进行补救
	cin.get(ch);
	3.使用哪一个cin.get()
	cin.get(ch, size).get();
	4.文件尾条件
	cin.fail() 即 eof()
	5.另一个cin.get()版本
	cin,get()
	*/

	return 0;
}