#include <iostream>
using namespace std;

int main()
{
	/*
	for循环
	*/

	/*
	for循环的组成部分
	for(initialization; test-expression; update-expression)
		body
	*/

	/*
	前缀格式和后缀格式
	for(n = lim; n > 0; --n)
	for(n = lim; n > 0; n--)
	对程序的行为没有什么影响 但执行速度可能有细微的差别
	*/

	/*
	其他语法技巧————逗号运算符
	示例：
	for(i=1, j=10; i < j; ++i, --j)
	*/

	/*
	赋值、比较和可能犯的错误
	不要混淆等于运算符(==)和赋值运算符(=)
	test-expression表达式的值为true或false
	*/

	return 0;
}