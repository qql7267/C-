#include <iostream>

int main()
{
	/*
	递归
	C++函数的一个特点——可以调用自己(然而 与C语言不同的是 C++不允许main调用自己)
	void类型的递归函数recurs()的代码如下
	void recurs(argumentlist)
	{
		statements1
		if (test)
			recurs(arguments)
		statements2
	}
	*/


	return 0;
}