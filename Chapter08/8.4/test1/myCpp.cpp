#include <iostream>

int main()
{
	/*
	函数重载
	可以使用多个同名但不同类型参数的函数 即多态
	函数重载的关键是函数的参数列表 也被称为函数特征标(function signature) 
	如果两个函数的参数数目和类型相同 同时参数的排列顺序也相同 则他们的特征标相同
	如果不同 则特征标不同 调用是会匹配使用的特征标 不同的特征标就是不同的函数
	*/

	return 0;
}